\documentclass{resume}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[french]{babel} 

\usepackage{lmodern}
\usepackage{microtype}

\usepackage[left=0.4in,top=0.4in,right=0.4in,bottom=0.4in]{geometry}

\name{ {{ name }} }
\address{ {{ phone }} \\ {{ city }} }
\address{ \href{mailto:{{ email }}}{ {{ email }} } \\ \href{ {{ website }} }{ {{ website }} } }

\begin{document}

\begin{rSection}{OBJECTIVE}
{{ objective }}
\end{rSection}

\begin{rSection}{EDUCATION}
{% for edu in education %}
{\bf {{ edu.degree }}}, {{ edu.institution }} \hfill {{ edu.year }}\\
Relevant Coursework: {{ edu.coursework }}.
{% endfor %}
\end{rSection}

\begin{rSection}{SKILLS}
\begin{tabular}{ @{} >{\bfseries}l @{\hspace{6ex}} l }
Technical Skills & {{ skills.technical | join(', ') }} \\
Soft Skills & {{ skills.soft | join(', ') }} \\
Tools & {{ skills.tools | join(', ') }} \\
\end{tabular}
\end{rSection}

\begin{rSection}{EXPERIENCE}
{% for exp in experience %}
\textbf{ {{ exp.title }} } \hfill {{ exp.start_date }} - {{ exp.end_date }}\\
{{ exp.company }} \hfill \textit{ {{ exp.location }} }
\begin{itemize}
{% for bullet in exp.bullets %}
\item {{ bullet }}
{% endfor %}
\end{itemize}
{% endfor %}
\end{rSection}

\begin{rSection}{PROJECTS}
{% for proj in projects %}
\item \textbf{ {{ proj.title }} } { {{ proj.description }} {% if proj.link %} \href{ {{ proj.link }} }{(Link)} {% endif %} }
{% endfor %}
\end{rSection}

\begin{rSection}{EXTRA-CURRICULAR ACTIVITIES}
\begin{itemize}
{% for activity in extra_curricular %}
\item {{ activity }}
{% endfor %}
\end{itemize}
\end{rSection}

\end{document}
