import { JobAddressPipe } from './job-address-pipe';

describe('JobAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new JobAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
